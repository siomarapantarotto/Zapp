<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg"
    tools:context="br.com.siomara.zapp.activity.ChatActivity">

    <include
        android:id="@+id/toolbarChatsID"
        layout="@layout/toolbar"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"></include>


    <ListView
        android:id="@+id/listChatsID"
        android:layout_width="match_parent"
        android:layout_height="460dp"
        android:layout_marginTop="2dp"
        android:padding="8dp"
        app:layout_constraintTop_toBottomOf="@+id/toolbarChatsID"
        tools:layout_editor_absoluteX="-1dp"></ListView>

    <EditText
        android:id="@+id/edtMessageID"
        android:layout_width="293dp"
        android:layout_height="36dp"
        android:layout_marginBottom="6dp"
        android:layout_marginStart="6dp"
        android:layout_weight="1"
        android:background="@color/colorAccent"
        android:hint="Type message..."
        android:paddingHorizontal="2dp"
        android:paddingVertical="2dp"
        android:textColor="@android:color/black"
        android:textColorHint="@android:color/darker_gray"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"></EditText>

    <ImageButton
        android:id="@+id/btnSendID"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="2dp"
        android:src="@drawable/ic_action_send"
        android:theme="@style/ButtonStyle"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"></ImageButton>


</android.support.constraint.ConstraintLayout>
